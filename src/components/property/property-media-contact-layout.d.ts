import type { ReactNode } from "react";

export type PropertyMediaContactLayoutProps = {
  media: ReactNode;
  mobileContact: ReactNode;
  details: ReactNode;
  sidebar: ReactNode;
};

export function PropertyMediaContactLayout(props: PropertyMediaContactLayoutProps): ReactNode;
