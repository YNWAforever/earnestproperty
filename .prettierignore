node_modules
dist
.output
.vinxi
pnpm-lock.yaml
package-lock.json
routeTree.gen.ts

# Verbatim pages scraped from the old site, used as parser fixtures. Prettier
# cannot parse them (unterminated <LINK> tags and similar), and reformatting
# would invalidate the very markup the migration tests assert against.
scripts/old-site-migration/__fixtures__

# Verbatim 28Hse pages captured as parser test fixtures. Same rationale as
# above -- reformatting would rewrite the literal HTML the MLS parser tests
# assert against.
src/lib/mls/__fixtures__/28hse

# Dated plan/spec/report artifacts. These are records of what was decided at a
# point in time; prettier rewrites the code samples embedded in them, which
# changes the record for no benefit.
docs/superpowers

# Generated by external tooling (Lovable, superpowers); rewritten by those tools
# on their own schedule, so formatting them here just creates churn.
.lovable
.superpowers
