# Property conversion suite deployment

Run `npm run neon:migrate` in the target environment before enabling the full public agent profiles and agent CMS.

This rollout keeps unpublished agents hidden while the migration is pending. The live migration is an explicit deployment step and is not run as part of this code change.
